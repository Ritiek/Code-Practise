fn longest_consec(strarr: Vec<&str>, k: usize) -> String {
	for i in 0..(strarr.len()+1) {
		for j in 1..k+1 {
			
		}
	}
}

fn main() {
	let strarr = vec!["zone", "abigail", "theta", "form", "libe", "zas"];
	let result = longest_consec(strarr);
	println!("{}", result);
}
